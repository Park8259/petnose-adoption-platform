# Optional dependencies for DOG_NOSE_RUNTIME=onnxruntime and ONNX export checks.
# Keep separate from the default mock image; install only for runtime experiments.

numpy==1.26.4
Pillow==10.3.0
onnx==1.16.1
onnxruntime==1.18.1
