{
  "name": "petnose-firebase-rules",
  "version": "0.1.0",
  "private": true,
  "description": "Emulator-only Firestore security rules tests for PetNose Firebase chat.",
  "type": "commonjs",
  "scripts": {
    "test": "firebase emulators:exec --config ../../firebase.json --project petnose-rules-test --only firestore \"node --test firestore.rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.13.0",
    "firebase-tools": "^15.18.0"
  }
}
